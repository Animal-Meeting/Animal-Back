spring:
  profiles:
    active: dev
    group:
      dev: "dev"
      prod: "prod"
    include:
      - infra

server:
  port: 8000
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

